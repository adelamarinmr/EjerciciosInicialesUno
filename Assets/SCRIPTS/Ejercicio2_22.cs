using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicio_2 : MonoBehaviour
{
    int edad, codigoPostal, numeroDeHijos, tallaCamisa, mayorEdad,minutos,dias;
    float altura, iva, precio, contador,peso;
    string nombre, mensaje, matriculaCoche;
    bool mayorDeEdad,alumnoRepetidor;
    char letra, genero,tallaCamiseta;



   







    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
